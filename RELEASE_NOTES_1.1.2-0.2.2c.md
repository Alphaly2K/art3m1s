# Art3m1s 1.1.2-0.2.2c

本版本配套 `art3m1s-core 0.2.2`，重点改善移动端操作、翻译资料管理、跨平台数据目录和
Apple 平台发布可靠性。

## 主要变化

- **移动触摸板**：Android 与 iOS 可启用相对移动模式。手指滑动控制舞台光标，轻点执行
  左键，长按后移动执行拖拽；光标改为带黑色描边的标准左上箭头，不再留下阴影印记。
- **每游戏翻译补丁**：本地对照文件移入项目编辑页，每个游戏保存自己的补丁路径；在线
  翻译服务配置仍由应用统一管理。
- **Protobuf 翻译缓存**：在线译文缓存使用二进制 Protobuf，减少解析和持久化开销。
- **统一应用数据目录**：存档、翻译缓存与封面现在从同一个平台应用数据根目录派生。
  macOS 使用应用支持目录，iOS 使用文档目录，Android 使用应用外部数据目录，并迁移
  已知旧布局。
- **无缝分段 BGM**：Artemis 常见的 `*_a` 引导段和 `*_b` 循环段在同一原生播放列表中
  衔接，避免 Dart 完成回调造成的静音间隔。
- **Apple 构建改进**：macOS Release 正确加载 bundle 内 PFS 与 ANGLE library，并保留
  已导入外部游戏的访问能力；iOS 构建脚本支持 arm64 真机和 Simulator XCFramework，
  也可仅输出真机切片。
- **输入对话框稳定性**：对话框自行管理输入 controller 生命周期，iOS 提交时不再因退出
  动画期间收到输入法更新而触发 Flutter assert。Artemis 允许空字符串，宿主保持该语义。

## 升级说明

- 需要同时更新至 `art3m1s-core 0.2.2`。
- 旧 JSON 在线翻译缓存不做迁移；游戏再次产生在线译文时会建立新的 `.pb` 缓存。
- 首次启动会尝试把旧目录中的存档、翻译缓存和封面复制到统一数据目录；已有目标文件不会
  被覆盖。

## 构建产物

- macOS Release App
- Android Release APK
- iOS 真机 Release App/IPA（未签名，供发布者后续签名）

iOS Simulator 支持保留在源码与构建脚本中，但本次发布附件不包含 Simulator 切片。
